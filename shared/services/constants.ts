export enum ApiRoutes {
  SEARCH_PRODUCTS = "/products/search",
  INGREDIENTS = "/ingredients",
  GET_CART = "/cart",
  POST_CART = "/cart",
  AUTH_ME = "/auth/me",
  // PATCH_CART = `/cart/${id}`,
}
